# 更新日志
### 2025-12-26

- 页面布局完成，上侧菜单栏，中部展示区，底部备案区
- 主页布局完成，顶部数据看板，然后是平台和数据集介绍，然后是三种饼图，然后是可视化图表
- 全站中英文转换完成
- 整体采用tailwindcss和nativeui组件完成，所以整体风格和nativeui的风格类似，白绿色主题，目前看起来还算好看

#### 项目结构

```
TCM_benchmark_frontend/
├── public/                     # 静态资源
│   ├── dataset_classify.png    # 数据集分类图
│   ├── val_test.png            # 验证测试图
│   ├── xuewei_classify.png     # 穴位分类图
│   └── ...
├── src/                        # 源代码
│   ├── assets/                 # 资源文件
│   ├── components/             # 公共组件
│   │   └── Navbar.vue          # 导航栏组件
│   ├── locales/                # 国际化配置
│   │   ├── en-US.js            # 英文语言包
│   │   ├── zh-CN.js            # 中文语言包
│   │   └── index.js            # i18n入口
│   ├── router/                 # 路由配置
│   │   └── index.js            # 路由定义
│   ├── stores/                 # 状态管理
│   │   └── app.js              # 全局应用状态
│   ├── views/                  # 页面视图
│   │   ├── datasets/           # 数据集页面
│   │   │   └── index.vue
│   │   ├── home/               # 主页
│   │   │   └── index.vue
│   │   ├── leaderboard/        # 排行榜页面
│   │   │   └── index.vue
│   │   └── index.vue           # 主布局
│   ├── App.vue                 # 根组件
│   ├── main.js                 # 入口文件
│   └── style.css               # 全局样式
├── index.html                  # HTML入口
├── package.json                # 项目配置
├── vite.config.js              # Vite配置
└── 更新日志.md                 # 更新记录
```

### 2025-12-24

- 初始化项目Vite+vue3+JS
- 安装vue-route，路由
- 安装pinia，状态管理
- 安装tailwindcss4+，工具CSS
- 安装naiveui组件库
- 安装vue-data-ui
- 配置了Components与AutoImport自动导入
- 初始化github仓库
